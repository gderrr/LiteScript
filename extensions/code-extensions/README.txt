Here we provide a VSCode extension useful for correctly indenting code and for syntax highlighting.

To install it, just simply install the .vsix file in a folder and run the following command:

code --install-extension litescript-0.1.8.vsix